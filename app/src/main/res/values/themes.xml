<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BleBulbTest" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 隐藏状态栏 -->
        <!--        <item name="android:windowFullscreen">true</item>-->
        <!-- 隐藏标题栏 -->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColorPrimary">@color/nav</item>
        <item name="android:textColor">@color/nav</item>
        <item name="colorPrimary">@color/background_grey</item>
        <item name="colorPrimaryDark">@color/background_grey</item>
        <item name="colorAccent">@color/background_grey</item>

    </style>
</resources>